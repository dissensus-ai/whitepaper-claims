# Virtual environments
.venv-*/
venv/
.venv/
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/

# LaTeX build artifacts
*.log
*.aux
*.bbl
*.blg
*.out
*.fls
*.fdb_latexmk
*.synctex.gz
*.toc
*.lof
*.lot
*.run.xml
*.bcf
*-blx.bib

# Generated PDFs (keep source, regenerate from LaTeX)
paper/*.pdf

# arXiv submission archive (keep source, not archive)
paper/*.tar.gz

# Large tensor outputs (regenerated by pipeline)
outputs/tensor/*.npy
# ...but keep the small CP factor artifact required by run_alignment.py on a clean clone
# (run_full_pipeline.py regenerates it; this is a clean-clone convenience, not a large blob)
!outputs/tensor/cp_asset_factors.npy

# IDE / Editor
.idea/
.vscode/
*.swp
*~

# System
.DS_Store
Thumbs.db

# Claude-specific (internal tooling)
.claude/
.claude-flow/
.swarm/
paper/.claude-flow/

# Internal rebuild scaffolding (Jun 2026) — NOT for publication
data/_quarantine_broken_*/
outputs/_pre_rerun_backup_*/
outputs/nlp/_stale_n38_superseded/
*.bak
*.bak-*
.pytest_cache/
